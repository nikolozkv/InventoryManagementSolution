@model InventoryManagementWebApp.Models.Company
@{
    ViewData["Title"] = "კომპანიის არქივირება";
}

<div class="container py-2">
    <div class="row justify-content-center">
        <div class="col-lg-7 col-md-9 col-sm-12">
            <div class="card shadow-lg border-0 rounded-3">
                <div class="card-header bg-danger text-white p-3 rounded-top-3 d-flex align-items-center justify-content-center">
                    <h2 class="card-title mb-0 fs-3">
                        <span class="fw-bold">🗑️ კომპანიის არქივირება</span>
                    </h2>
                </div>
                <div class="card-body p-4">
                    
                    <div class="alert alert-warning d-flex align-items-center mb-4" role="alert">
                        <i class="bi bi-exclamation-triangle-fill flex-shrink-0 me-2 fs-4"></i>
                        <div>
                            <strong>გაფრთხილება!</strong> დარწმუნდით, რომ გსურთ ამ კომპანიის არქივირება.
                        </div>
                    </div>

                    <dl class="row mb-4">
                        <dt class="col-sm-4 text-muted">კომპანიის სახელი:</dt>
                        <dd class="col-sm-8 fw-bold">@Model.Name</dd>

                        <dt class="col-sm-4 text-muted">საიდენტიფიკაციო კოდი:</dt>
                        <dd class="col-sm-8">@Model.IdentifierCode</dd>

                        <dt class="col-sm-4 text-muted">კონტაქტი:</dt>
                        <dd class="col-sm-8">@Model.ContactInfo</dd>

                        <dt class="col-sm-4 text-muted">მისამართი:</dt>
                        <dd class="col-sm-8">@Model.Address</dd>
                    </dl>

                    <form asp-action="CompanyArchive" method="post" class="col-12 text-center pt-3 border-top">
                        <input type="hidden" asp-for="CompanyID" />
                        <div class="d-flex justify-content-center flex-wrap gap-3">
                            <button type="submit" class="btn btn-danger btn-lg px-4">
                                <i class="bi bi-archive-fill me-2"></i> არქივირება
                            </button>
                            <a asp-action="Index" class="btn btn-secondary btn-lg px-4">
                                <i class="bi bi-arrow-left-circle-fill me-2"></i> უკან
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>